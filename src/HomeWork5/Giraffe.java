package HomeWork5;

public class Giraffe extends WildAnimal {
    public Giraffe(String id, int age, double weight, String color, boolean isPredator) {
        super(id, age, weight, color, isPredator);
    }
}
